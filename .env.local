# Payload CMS
PAYLOAD_SECRET=your-secret-key-here-change-in-production

# PostgreSQL (matches docker-compose.yml)
DATABASE_URL=postgresql://payload:payload_password@localhost:5432/appshift

# Seed script admin password
SEED_ADMIN_PASSWORD=changeme-strong-password
